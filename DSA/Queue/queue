#include<list>
class queue
{
private:
    list<int> l;
public:
    void enqueue(int val)
    {
        l.push_back(val);
    }
    int front()
    {
        return l.front();
    }
    void dequeue()
    {
        if(l.empty()) throw std::runtime_error("Queue is empty");
        l.pop_front();
    }
    bool empty()
    {
        return l.empty();
    }
};