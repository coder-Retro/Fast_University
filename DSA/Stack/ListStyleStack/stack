#include<list>
class stack
{
private:
    std::list<int> l;
public:
    void push(int n){ l.push_front(n); }
    void pop(){ l.pop_front(); }
    int top(){ return l.front(); }
    bool empty(){ return l.empty(); }
};